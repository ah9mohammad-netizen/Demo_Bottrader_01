services:
  - type: web
    name: crypto-signal-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: SESSION_STRING
        sync: false
